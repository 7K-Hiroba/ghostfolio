suite: HTTPRoute
templates:
  - templates/httproute.yaml
tests:
  - it: renders with default values
    asserts:
      - isKind:
          of: HTTPRoute
      - equal:
          path: apiVersion
          value: gateway.networking.k8s.io/v1

  - it: sets parent gateway reference
    set:
      gateway.parentRefs:
        - name: my-gateway
          namespace: gateway-system
    asserts:
      - equal:
          path: spec.parentRefs[0].name
          value: my-gateway
      - equal:
          path: spec.parentRefs[0].namespace
          value: gateway-system

  - it: uses ghostfolio.example.com hostname by default
    asserts:
      - equal:
          path: spec.hostnames[0]
          value: "ghostfolio.example.com"

  - it: sets custom hostnames
    set:
      gateway.hostnames:
        - finance.example.com
        - ghostfolio.internal.example.com
    asserts:
      - equal:
          path: spec.hostnames[0]
          value: "finance.example.com"
      - equal:
          path: spec.hostnames[1]
          value: "ghostfolio.internal.example.com"

  - it: renders catch-all rule when no custom rules
    set:
      gateway.rules: []
    asserts:
      - equal:
          path: spec.rules[0].matches[0].path.type
          value: PathPrefix
      - equal:
          path: spec.rules[0].matches[0].path.value
          value: /
