# Platform chart for Ghostfolio
# Provisions databases, secrets, and observability resources that the base
# chart's Deployment consumes but does not manage.
#
# Each resource supports a `provider` switch to select the backend implementation.
# Available providers are listed per resource — add your own by creating a
# matching template file in templates/<category>/<resource>-<provider>.yaml
#
# Templates are organized into subdirectories:
#   database/      — Database resources (CNPG, Dragonfly, etc.)
#   storage/       — Object storage (Crossplane, Garage, etc.)
#   secrets/       — External secrets management
#   observability/ — ServiceMonitors, Grafana dashboards, PrometheusRules

global:
  # -- Application name, used as prefix for all platform resources
  appName: ghostfolio
  # -- Release name of the base chart deployment. When set, the ServiceMonitor
  # selector matches `app.kubernetes.io/instance: <baseInstance>` so multiple
  # releases of the same app coexist safely. Leave empty to match by name only.
  baseInstance: ""

# -- PostgreSQL database (required by Ghostfolio)
postgres:
  enabled: true
  # -- Provider: "cnpg" (CloudNativePG operator)
  provider: cnpg
  instances: 1
  storage:
    size: 10Gi
    storageClass: ""
  resources:
    limits:
      cpu: "1"
      memory: 1Gi
    requests:
      cpu: 250m
      memory: 256Mi
  # -- PostgreSQL version
  imageName: ghcr.io/cloudnative-pg/postgresql:16.2
  # -- Database name to create
  database: ghostfolio
  # -- Database owner
  owner: ghostfolio
  # -- Backup configuration (optional)
  backup:
    enabled: false
    schedule: "0 2 * * *"
    retentionPolicy: "7d"

# -- Redis-compatible cache (required by Ghostfolio)
redis:
  enabled: true
  # -- Provider: "dragonfly" (Dragonfly operator — Redis-compatible)
  provider: dragonfly
  # -- Number of Dragonfly replicas
  replicas: 1
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # -- Dragonfly-specific settings
  dragonfly:
    # -- Dragonfly image (leave empty for operator default)
    image: ""
    # -- Extra arguments to pass to the Dragonfly process
    args: []
    # -- Whether to enable password authentication
    authentication:
      enabled: false
      # -- Reference to an existing Secret with a password key.
      # If empty, the operator generates one.
      passwordSecretRef:
        name: ""
        key: password

# -- S3-compatible object storage
s3:
  enabled: false
  # -- Provider: "crossplane" | "garage"
  provider: crossplane
  # -- Bucket name (will be prefixed with app name)
  bucketName: assets
  # -- Access control
  acl: private

  # -- Crossplane-specific settings (provider: crossplane)
  crossplane:
    region: us-east-1
    providerConfigRef: aws-provider
    lifecycle:
      enabled: false
      expirationDays: 90

  # -- Garage-specific settings (provider: garage)
  garage:
    # -- Garage API endpoint
    endpoint: "http://garage.garage.svc.cluster.local:3900"
    # -- Garage access key Secret reference
    accessKeySecret:
      name: ""
      key: accessKey
    # -- Garage secret key Secret reference
    secretKeySecret:
      name: ""
      key: secretKey
    # -- Number of data/parity replicas in Garage
    replicationFactor: 3

# -- External Secrets (external-secrets.io)
# Use this to pull Ghostfolio's required secrets (DATABASE_URL, JWT_SECRET_KEY,
# ACCESS_TOKEN_SALT, REDIS_PASSWORD) from your secret backend.
externalSecrets:
  enabled: false
  # -- How often to sync secrets
  refreshInterval: 1h
  # -- SecretStore or ClusterSecretStore reference
  storeRef:
    name: cluster-secret-store
    kind: ClusterSecretStore
  # -- Individual secret mappings
  data: []
  #  - secretKey: DATABASE_URL
  #    remoteKey: ghostfolio/database
  #    property: url
  #  - secretKey: REDIS_HOST
  #    remoteKey: ghostfolio/redis
  #    property: host
  #  - secretKey: REDIS_PASSWORD
  #    remoteKey: ghostfolio/redis
  #    property: password
  #  - secretKey: ACCESS_TOKEN_SALT
  #    remoteKey: ghostfolio/auth
  #    property: access-token-salt
  #  - secretKey: JWT_SECRET_KEY
  #    remoteKey: ghostfolio/auth
  #    property: jwt-secret-key
  # -- Pull all keys from a remote path
  dataFrom: []
  #  - extract:
  #      key: ghostfolio/config
  # -- Optional target Secret template for value transformation.
  # See https://external-secrets.io/latest/guides/templating/
  target:
    template: {}
    # type: Opaque
    # data:
    #   DATABASE_URL: "postgresql://{{ `{{ .username }}` }}:{{ `{{ .password }}` }}@ghostfolio-pg-rw:5432/{{ `{{ .database }}` }}"

# -- Observability resources
observability:
  # -- Prometheus ServiceMonitor
  serviceMonitor:
    enabled: false
    # -- Additional labels for ServiceMonitor discovery (e.g., release: kube-prometheus-stack)
    additionalLabels: {}
    # -- Service port name to scrape
    port: http
    # -- Metrics endpoint path
    path: /metrics
    # -- Scrape interval
    interval: 30s
    # -- Scrape timeout
    scrapeTimeout: 10s

  # -- Grafana dashboard (deployed as ConfigMap with sidecar label)
  grafanaDashboard:
    enabled: false
    # -- Grafana folder label
    folderLabel: ""

  # -- PrometheusRule alerts. `groups` is passed through `tpl` so values can
  # reference `.Release.Namespace`, `include "platform.name" .`, etc. Override
  # entirely to replace the defaults, or append to extend them.
  prometheusRules:
    enabled: false
    groups:
      - name: '{{ include "platform.name" . }}.rules'
        rules:
          - alert: HighErrorRate
            expr: |
              sum(rate(http_requests_total{namespace="{{ .Release.Namespace }}", service="{{ include "platform.name" . }}", status=~"5.."}[5m]))
              /
              sum(rate(http_requests_total{namespace="{{ .Release.Namespace }}", service="{{ include "platform.name" . }}"}[5m]))
              > 0.05
            for: 5m
            labels:
              severity: warning
            annotations:
              summary: 'High error rate for {{ include "platform.name" . }}'
              description: "Error rate is above 5% for the last 5 minutes."
          - alert: HighLatency
            expr: |
              histogram_quantile(0.99, rate(http_request_duration_seconds_bucket{namespace="{{ .Release.Namespace }}", service="{{ include "platform.name" . }}"}[5m]))
              > 1
            for: 5m
            labels:
              severity: warning
            annotations:
              summary: 'High p99 latency for {{ include "platform.name" . }}'
              description: "p99 latency is above 1 second for the last 5 minutes."
